name: Check code when push

on:
  push:
    branches:
      - "main"

jobs:
  check-code:
    name: Check code
    runs-on: Ubuntu-20.04
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2
      - run: npm install
      - run: npm run ci
